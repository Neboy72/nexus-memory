__pycache__/
*.pyc
*.egg-info/
dist/
build/
# Secrets: ignore every env-file variant; only the example is committed.
.env
.env.*
!.env.example
*.env
plugins/openclaw/node_modules/
plugins/openclaw/dist/

# macOS
.DS_Store
.venv/
venv/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
*.log

# Backups (local runtime data)
backups/


# Logo drafts
docs/logo/

# Nexus snapshots / local backups (never in git) — ANCHORED to the repo root:
# unanchored `2026*_*` / `*-bak-*` also untracked any source/docs/fixture path
# that merely contained "-bak-" or started with "2026" anywhere in the tree.
/2026*_*
/2026*_*.md
/*-bak-*
/*.bak-*
__pycache__/
*.egg-info/

# Accidental '~' directory from backup script bug
~/
plugins/openclaw/*.tgz
plugins/openclaw/reports/
